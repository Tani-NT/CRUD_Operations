<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full h-full text-white bg-zinc-900">
        <form action="/update/<%= user._id %>" method="post" class="p-5">
            <h1 class="text-[2vw] mb-3">Update User</h1>
            <input value="<%= user.name %>" type="text" placeholder="name" name="name" class="px-3 py-2 rounded-md bg-transparent outline-none border-2 border-zinc-800">
            <input value="<%= user.email %>" type="email" placeholder="email" name="email" class="px-3 py-2 rounded-md bg-transparent outline-none border-2 border-zinc-800">
            <input value="<%= user.image %>" type="url" placeholder="image" name="image" class="px-3 py-2 rounded-md bg-transparent outline-none border-2 border-zinc-800">
            <input type="submit" class="px-3 py-2 bg-yellow-500 text-black rounded-md ml-3 cursor-pointer" value="Update User">
        </form>
        <a href="/read" class="tracking-tighter text-blue-500 ml-5 hover:text-zinc-600">show users</a>
    </div>
</body>
</html>